<?xml version="1.0" encoding="utf-8"?>
<!-- Sideload-only manifest additions: everything the in-app self-updater needs. The Play flavor
     merges NONE of this — Play forbids self-updating apps, and REQUEST_INSTALL_PACKAGES alone
     draws review scrutiny. -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <application>
        <!-- Shares the downloaded update APK with the system installer. -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>
</manifest>
